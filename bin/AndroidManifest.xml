<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.com.spinninghalf.connectingtothenetwork"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    
    <uses-sdk android:minSdkVersion="7"
        android:targetSdkVersion="15" />
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".SpinningHalfApplication" >
        <activity
            android:name=".HttpExampleActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Sherlock" >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GigListActivity" />
        <activity android:name=".ViewGig" />
        <activity android:name=".DownloadAndParseGigs" />
        <activity android:name=".MainFragmentGigListActivity"></activity>
        <service android:name=".GigUpdaterService" />
    </application>

</manifest>