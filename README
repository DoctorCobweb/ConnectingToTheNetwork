prototype android app which talks to a google app engine RESTful web service which is outputtin xml from its datastore.

it uses http as the protocol and XmlPullParser to parse the document.

more soon dudes.
